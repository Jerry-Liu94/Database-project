server start

uvicorn main:app --reload

安裝requirement

pip install -r requirements.txt

匯出requirement

pip freeze > requirements.txt

建立虛擬環境

windows
py -m venv venv 

mac
python3 -m venv venv

啟動
windows

venv\Scripts\activate

mac 

. venv/bin/activate

離開

deactivate

網站:http://127.0.0.1:8000/docs

# 1. 停止服務
sudo systemctl stop redant

# 2. 確認沒有殘留的 Python 程序 (如果有的話，用 kill 殺掉)
ps aux | grep uvicorn

# 3. 啟動服務
sudo systemctl start redant